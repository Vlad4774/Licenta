services:
  - type: web
    name: saleshub
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput

    startCommand: |
      python manage.py migrate
      gunicorn saleshub.wsgi:application

    staticPublishPath: staticfiles
